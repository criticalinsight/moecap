{
    "name": "moecap",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "test": "bun test",
        "test:coverage": "bun test --coverage",
        "build": "bun run scripts/build-site.ts",
        "deploy": "bun run build && npx wrangler pages deploy public --project-name moecap"
    },
    "devDependencies": {
        "bun-types": "latest"
    }
}